# iisc_capstone Project Repository

This repository contains the implementation of a multi-dialog chatbot system integrated with FAISS-based retrieval and RAG (Retrieval-Augmented Generation) for intent detection.

## 📂 Folder Structure

- `data/`
  - Contains source data files, embeddings, or datasets used in the project.

- `faiss_index/`
  - Stores FAISS index files generated for similarity search.

- `scripts/`
  - Contains Python utility scripts and supporting modules used by the chatbot.

- `chatbot_app_v3_multidialog_with_IndigoRAG_Intent_working.py`
  - Main Python application file for the chatbot system.

## 📌 How to Run

1. Install required packages from `requirements.txt` (if available).
2. Make sure the FAISS index files are generated and present in `faiss_index/`.
3. Run the main chatbot script:
